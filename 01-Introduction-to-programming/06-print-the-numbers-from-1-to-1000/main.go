package main

import "fmt"

// Create a program, which prints the numbers from 1 to 1000.
// The numbers should be printed on different lines.
// For this problem, you will have to do some research in order to avoid having to print them all by hand.

func main() {
	/* for i := 1; i <= 1000; i++ {
		fmt.Println(i)
	} */
	j := 1
	for j <= 1000 {
		fmt.Println(j)
	}
}
